# 🟣 codex·cu-luna-xcode — Computer-Use-Selbsttest + Xcode-Accounts-Inspektion

Du bist Codex mit Modell gpt-5.6-luna und dem computer-use MCP (SkyComputerUseClient). Arbeite autonom, stoppe nicht für Rückfragen.

## 🔴 HARD-Guardrails
- Du tippst NIEMALS Apple-ID-Passwörter oder 2FA-Codes — nirgends. Wenn ein Login-Feld erscheint: Screenshot machen, dokumentieren, NICHT ausfüllen.
- Keine Käufe, nichts löschen, keine Systemeinstellungen ändern (außer nichts).

## Aufgaben (der Reihe nach)
1. **CU-Selbsttest (Pflicht-Gate):** Mit computer-use MCP Finder oder Notes öffnen/lesen (App-Name + UI-Zustand muss zurückkommen). Dann Datei `~/Desktop/cu_test_eu.txt` erzeugen mit exakt: `Test erfolgreich – Computer Use läuft in EU` — per Shell verifizieren, danach nochmal per CU Finder lesen.
2. **Xcode öffnen** → Menü Xcode → Settings → Accounts. Screenshot des Accounts-Tabs speichern nach `/Users/mh/orca/workspaces/sonar-live-audio/evidence/ios-install/2026-08-09_xcode-accounts.png`.
3. **Dokumentiere exakt:** Welche Apple-IDs/Teams sind eingetragen? (Erwartet: martin.hausleitner@icloud.com / Team H2768P4284. Felix' Apple-ID vermutlich FEHLT.) Falls eine fehlt: notiere die exakten Klickschritte für den Operator ("+ unten links → Apple Account → …").
4. **Ergebnis** nach `/Users/mh/orca/workspaces/sonar-live-audio/briefs/cu-luna-xcode.RESULT.md`: CU-Selbsttest ✅/🔴 (mit Beweis), Accounts-Liste, Screenshot-Pfad, Operator-Klickschritte.

Fertig = RESULT.md geschrieben + Screenshot existiert.
