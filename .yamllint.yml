extends: default

ignore: |
  .claude/
  build/
  releases/

rules:
  document-start: disable
  line-length:
    max: 140
    level: warning
  truthy:
    allowed-values: ["true", "false", "on", "off"]
